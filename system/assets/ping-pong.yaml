apiVersion: v1
kind: Namespace
metadata:
  name: test
---
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: rollouts-demo
  namespace: test
spec:
  replicas: 2
  strategy:
    canary:
      canaryService: nginx-preview # our created canary service
      pingPong:
        pingService: nginx-ping
        pongService: nginx-pong
      scaleDownDelayRevisionLimit: 1
      scaleDownDelaySeconds: 30
      steps:
        - setWeight: 0
        - setCanaryScale:
            replicas: 2
        - pause: {}
        - setWeight: 100
      trafficRouting:
        alb:
          ingresses:
            - rollouts-demo-ingress
          rootService: nginx-pingpong-stable
          servicePort: 80
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: http-nginx
  template:
    metadata:
      labels:
        app: http-nginx
      annotations:
        linkerd.io/inject: enabled
    spec:
      containers:
        - name: http-nginx
          image: nginx:latest
          ports:
            - containerPort: 80
              protocol: TCP
              name: http
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-preview
  namespace: test
  labels:
    app.kubernetes.io/name: http-nginx
    app.kubernetes.io/instance: http-nginx
    app.kubernetes.io/version: "1.0.0"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: http-nginx
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-ping
  namespace: test
  labels:
    app.kubernetes.io/name: http-nginx
    app.kubernetes.io/instance: http-nginx
    app.kubernetes.io/version: "1.0.0"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: http-nginx
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-pong
  namespace: test
  labels:
    app.kubernetes.io/name: http-nginx
    app.kubernetes.io/instance: http-nginx
    app.kubernetes.io/version: "1.0.0"
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: http-nginx
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rollouts-demo-ingress
  namespace: test
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
spec:
  rules:
    - host: test.jnytnai.click
      http:
        paths:
          - path: /*
            pathType: ImplementationSpecific
            backend:
              service:
                name: nginx-pingpong-stable
                port:
                  name: use-annotation
          - path: /internal/*
            pathType: ImplementationSpecific
            backend:
              service:
                name: nginx-preview
                port:
                  number: 80
